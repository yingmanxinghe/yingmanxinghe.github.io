# 基础配置
source 'https://rubygems.org'

# Jekyll 主程序
gem 'jekyll', '~> 4.0'

# 分页插件（必须单独声明）
gem 'jekyll-paginate'

# Windows 下需要额外的 webrick 依赖
gem 'webrick'

# Windows 文件监控优化
gem 'wdm', '>= 0.1.0' if Gem.win_platform?